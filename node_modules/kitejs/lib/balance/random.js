"use strict";
/**
 * @author xiangshouding
 */
exports.__esModule = true;
function random(hosts) {
    var len = hosts.length;
    return hosts[Math.floor(Math.random() * len)];
}
exports.random = random;
