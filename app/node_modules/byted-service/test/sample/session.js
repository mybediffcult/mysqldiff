const {Session} = require('../../');
const now = Session.session({
    psm: 'toutiao.passport.session_v2',
    expire: 20,
    consul: {
        host: '10.3.23.41',
        port: 2280
    }
});

console.log(now.getSessionKey());

async function task() {
    await now.set('name', 'bytedance');
    var name = await now.get('name');
    console.log(name);
}


task().then(function () {
}).catch(err => {
    throw err;
});
